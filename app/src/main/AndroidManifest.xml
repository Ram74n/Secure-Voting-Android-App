<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecureAppVoting">

        <!-- Results Activity -->
        <activity
            android:name=".activities.ResultsActivity"
            android:exported="false" />

        <!-- Create Poll Activity -->
        <activity
            android:name=".activities.CreatePollActivity"
            android:exported="false" />

        <!-- Voting Activity -->
        <activity
            android:name=".activities.VoteActivity"
            android:exported="false" />

        <!-- Dashboard Activity (after login) -->
        <activity
            android:name=".activities.DashboardActivity"
            android:exported="false" />

        <!-- Login Activity (Launcher) -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false" />

        <!-- Main Activity (not used for launching) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>

</manifest>
